# inventories/demo/hosts.yaml
# SECloudit v2.0 demo 환경 인벤토리

all:
  children:
    console-vm:
      hosts:
        console-01:
          ansible_host: "{{ console_vm_ip }}"  # TODO: 실제 IP 확인 필요
          ansible_user: root
    k8s-master:
      hosts:
        k8s-master-01:
          ansible_host: "{{ k8s_master_ip }}"  # TODO: Terraform output(master_ip)
          ansible_user: root
