# 세션 로그 2026-03-17

## #1
요청: velero 프로젝트로 마이그레이션 테스트
결과: 스크립트 버그 수정 후 2개 이미지 마이그레이션 성공 (100% 검증 통과)

---

## 세션 상세

### 발견 및 수정된 버그

| 버그 | 원인 | 수정 |
|------|------|------|
| CSV 파싱 오류 | CRLF 라인 엔딩 | `tr -d '\r'` 적용 |
| 스크립트 조기 종료 | `set -e` + `((count++))` 0 평가 | `((count++)) \|\| true` |
| docker pull 실패 | URL에 `https://` 포함 | `${VAR#https://}` 제거 |
| docker push 실패 | 대상 Harbor에 프로젝트 없음 | `create-sre-projects.sh` 실행 |
| `--project` 옵션 무시됨 | main loop에 필터 누락 | 필터 조건 추가 |

### 수정된 파일

1. `scripts/harbor-migrate.sh`
   - URL 프로토콜 prefix 제거 로직 추가 (line 30-33)
   - `((count++)) || true` 등 산술 연산 수정 (line 142, 155, 157)
   - `--project` 필터를 main loop에 추가 (line 137-140)
   - total 계산 시 TARGET_PROJECT 고려 (line 118-122)

2. `scripts/verify-digest.sh`
   - URL 프로토콜 prefix 제거 로직 추가 (line 23-28)
   - `((count++)) || true` 등 산술 연산 수정 (line 75, 79, 81)

3. `data/migrate_images.csv`
   - CRLF → LF 변환

### 테스트 결과

```
프로젝트: velero
이미지 수: 2개
마이그레이션: 성공 2, 실패 0
Digest 검증: 100% PASS

- velero/velero-plugin-for-aws:v1.9.0 (31.76MB, 10s)
- velero/velero:v1.17.1 (77.15MB, 19s)
```

---

## 세션 종료 메모

- **루프가 돌았던 순간**: `set -e`와 bash 산술 연산(`((count++))`)의 상호작용 디버깅에 여러 차례 시도
- **Claude가 틀린 경우**: 초기에 `sed -i 's/\r$//'`만 적용하여 줄 끝 CR만 제거했으나, 필드 중간의 CR은 남아있었음
- **다음에 개선할 것**: 스크립트 작성 시 `set -e` 환경에서 산술 연산은 처음부터 `|| true` 붙이기, CSV 생성 시 `tr -d '\r'` 파이프라인에 포함

---

## #2
요청: 전체 마이그레이션 파이프라인 실행
결과: 1,207개 이미지 중 868개 성공 (71.9%), 검증 80% PASS

---

## 세션 상세 (파이프라인 실행)

### 실행 단계

| 단계 | 작업 | 결과 |
|------|------|------|
| 1단계 | 이미지 분석 (quick-analyze.sh) | 마이그레이션 1,226개 / 백업 1,022개 |
| 2단계 | SRE Harbor 프로젝트 생성 | 59개 프로젝트 생성 |
| 3단계 | 백업 | 스킵 (/root 권한 문제) |
| 4단계 | 마이그레이션 | 성공 868 / 실패 122 / 스킵 217 |
| 5단계 | Digest 검증 (50개 샘플) | 40 PASS / 10 FAIL (80%) |
| 6단계 | 최종 보고서 | logs/pipeline-report.txt 생성 |

### 발생한 문제 및 해결

| 문제 | 원인 | 해결 |
|------|------|------|
| Push 실패 다수 발생 | 대상 Harbor에 프로젝트 없음 | source_projects.txt 재생성 후 59개 프로젝트 일괄 생성 |
| 백업 실패 | /root 디렉토리 권한 없음 (Windows) | 스킵 처리, 추후 경로 변경 필요 |
| 분석 스크립트 2개만 검출 | jq 파이프 처리 문제 (Windows/Git Bash) | quick-analyze.sh 새로 작성 (임시 파일 사용) |

### 생성/수정된 파일

- `scripts/quick-analyze.sh` - 새로 생성 (Windows 호환 분석 스크립트)
- `scripts/pipeline.sh` - 새로 생성 (전체 파이프라인 스크립트)
- `data/source_projects.txt` - 59개 프로젝트 목록으로 갱신
- `data/migrate_images.csv` - 1,226개 이미지 목록
- `data/backup_images.csv` - 1,022개 이미지 목록
- `logs/pipeline-report.txt` - 최종 보고서
- `logs/migrate-full-20260317_231951.log` - 마이그레이션 상세 로그

### 마이그레이션 통계

```
시작: 2026-03-17 23:20:01
완료: 2026-03-18 05:38:33
소요 시간: 약 6시간 18분

전체: 1,207개 (secloudit-helm 19개 제외)
성공: 868개 (71.9%)
실패: 122개 (10.1%)
스킵: 217개 (18.0%, 이전 실행에서 완료)
```

---

## 세션 종료 메모

- **루프가 돌았던 순간**: harbor-analyze.sh의 jq 파이프 문제로 여러 번 디버깅 반복
- **Claude가 틀린 경우**: 초기 Push 실패 원인을 즉시 파악하지 못함 (프로젝트 미생성)
- **다음에 개선할 것**: 
  - 마이그레이션 전 프로젝트 자동 생성 로직 추가
  - 백업 경로를 환경 변수로 유연하게 설정
  - 실패한 122개 이미지 재시도 스크립트 작성
