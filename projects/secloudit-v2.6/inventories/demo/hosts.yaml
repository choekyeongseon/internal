---
# inventories/demo/hosts.yaml
# Demo 환경 인벤토리

all:
  children:
    k8s_master:
      hosts:
        k8s-master:
          ansible_host: "{{ k8s_master_ip }}"  # TODO: k8s-cluster Terraform output 확인 필요
          ansible_user: root
          ansible_ssh_private_key_file: ~/.ssh/id_rsa
